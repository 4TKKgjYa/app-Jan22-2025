import { Form } from "~/components/ui/form";
import { useForm } from "react-hook-form";
import z from "zod";

export default function Index() {
  return (
    <div>
      <form action=""></form>
    </div>
  );
}
